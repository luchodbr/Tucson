import { CategoriaEnumPipe } from './categoria-enum.pipe';

describe('CategoriaEnumPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoriaEnumPipe();
    expect(pipe).toBeTruthy();
  });
});
